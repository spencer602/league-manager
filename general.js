function setTitle(titleString) {
    var titleTag = document.getElementById('pageTitle');
    titleTag.innerText = titleString;
}

function setCurrentPage(titleString) {
    var currentPage = document.getElementById('currentPage');
    currentPage.innerText = titleString;
}